namespace VellumPdfShowcase.Tests;

/// <summary>
/// Confirms that the xUnit v3 test host runs under the pinned SDK. The
/// Roslyn round-trip test and the capability table completeness test
/// belong to later steps of the implementation sequence and are not part
/// of this scaffold.
/// </summary>
public class ScaffoldTests
{
    [Fact]
    public void TestHostRuns()
    {
        Assert.True(true);
    }
}
